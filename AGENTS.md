`./CSP-Prover-5-1-2020` に Isabelle 2020 向けに実装された CSP の定義があります。
これを Lean4 向けに忠実に移植します。


# 移植時の遵守事項
## ディレクトリ階層
ディレクトリ階層は Lean4 に合わせて変更して構いません。


## ファイルの構成
ファイルの構成（定義・定理・補題）を維持してください。


## コメント
コメントは次の例外を除き内容を変えないでください。

* 例外1: Isabelle でないと意味を解釈できないコメントは Lean4 向けに変更してください


## 定義
定義は忠実に移植してください。
Isabelle にあって Lean にはない定義があった場合、別途ファイルを作ってそこに互換用の定義をまとめてください。


## 証明
定理、補題は維持してください。
それぞれの証明は Lean4 で証明が通るように変更してください。


# 作業手順
## 依存グラフの極小元から始める
CSP-Prover 内の依存グラフの極小元（CSP-Prover のどこにも依存していないファイル）から移植してください。
極小元は次のコマンドで取得可能です：

```console
$ cat ./DONE.txt | go run ./scripts/go/minimal/main.go | head -1
...
```

証明が成功し、移植が完了したら ./DONE.txt にそのファイルのファイルパス（e.g. `./CSP-Prover-5-1-2020/CSP/CSP.thy`）を追記し、そのファイルと DONE.txt を commit してください。
